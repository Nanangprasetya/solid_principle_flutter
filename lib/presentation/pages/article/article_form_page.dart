import 'package:flutter/material.dart';

class ArticleFormPage extends StatelessWidget {
  const ArticleFormPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Article Form Page"),
    );
  }
}
