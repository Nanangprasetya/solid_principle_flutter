import 'package:flutter/material.dart';

class PhotosFormPage extends StatelessWidget {
  const PhotosFormPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Photos Form Page"),
    );
  }
}
